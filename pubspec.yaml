name: navigation_exercise
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.1
  flutter_svg: ^1.1.4
  get_it: ^7.2.0
  injectable: ^1.5.3
  shared_preferences: ^2.0.15
  equatable: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner:
  injectable_generator: ^1.5.4

flutter:
  uses-material-design: true
  assets:
    - assets/
    - i18n/en.json
    - i18n/pl.json
